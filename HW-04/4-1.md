```c++
#include <stdio.h>

int main() {
    int days, sec ; //กำหนดตัวแปร
    printf("Input days : ");
    scanf("%d", &days); //รับค่าจำนวนวัน
    sec = days * 24 * 60 * 60; //แปลงวันเป็นวินาที
    printf("%d days = %d seconds\n", days, sec);
    return 0;
}
```
